# Local-only values. The project doesn't need to exist for `validate`; for a
# real `plan`/`apply` you'd set a project you own.
project_id          = "darkwolf-practice-dev"
region              = "us-east1"
event_topic_name    = "telemetry-events"
archive_bucket_name = "darkwolf-practice-dev-raw-archive"
